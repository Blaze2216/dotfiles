# ┌─────────────────────────────────────────────────────────────┐
# │                 Noctra OS - Keybindings                     │
# └─────────────────────────────────────────────────────────────┘

$mainMod = SUPER

# ── Core Applications ──
bind = $mainMod, Return, exec, kitty
bind = $mainMod, E, exec, thunar
bind = $mainMod, B, exec, firefox
# Fixed Rofi logic: Kill it if open, otherwise launch it
bind = $mainMod, A, exec, pkill rofi || rofi -show drun 
# Re-integrated the dynamic Matugen theme switcher we built
bind = $mainMod SHIFT, W, exec, ~/.config/hypr/scripts/theme-switcher.sh 

# ── System ──
bind = $mainMod, Q, killactive,
bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod, L, exec, loginctl lock-session
bind = $mainMod, R, exec, pkill -USR2 waybar
# ── Screenshots ──

# Print Screen -> Full Screenshot
bind = , Print, exec, ~/.config/hypr/scripts/screenshot.sh full

# Shift + Print Screen -> Select Area
bind = SHIFT, Print, exec, ~/.config/hypr/scripts/screenshot.sh area

# Super + Print Screen -> Select Area (Backup)
bind = $mainMod, Print, exec, ~/.config/hypr/scripts/screenshot.sh area

# ── Screen Recording ──

# Alt + Print -> Toggle Fullscreen Recording
bind = ALT, Print, exec, ~/.config/hypr/scripts/screen-record.sh full

# Super + Alt + Print -> Toggle Area Recording
bind = $mainMod ALT, Print, exec, ~/.config/hypr/scripts/screen-record.sh area

# ── Window Management ──
bind = $mainMod, V, togglefloating,
bind = $mainMod, P, pseudo,
bind = $mainMod, J, togglesplit,

# Move focus
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Move/resize windows with mouse
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# ── Workspaces ──
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to workspace
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Special Workspace (Scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through workspaces
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# ── Hardware & Media (SwayOSD) ──
# Volume
bindel = , XF86AudioRaiseVolume, exec, swayosd-client --output-volume raise
bindel = , XF86AudioLowerVolume, exec, swayosd-client --output-volume lower
bindl  = , XF86AudioMute, exec, swayosd-client --output-volume mute-toggle
bindl  = , XF86AudioMicMute, exec, swayosd-client --input-volume mute-toggle

# Brightness
bindel = , XF86MonBrightnessUp, exec, swayosd-client --brightness raise
bindel = , XF86MonBrightnessDown, exec, swayosd-client --brightness lower

# Media Player
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPrev, exec, playerctl previous


bind = $mainMod, Tab, exec, ~/.config/hypr/scripts/toggle-layout.sh
